---
"ketchup": minor
---

- Added automatic migration from the legacy `.claude-auto` directory to the new `.ketchup` directory on first plugin load after the rebrand
- Migration is safe and idempotent: skips when already migrated, and avoids overwriting if both directories exist
