---
"claude-auto": minor
---

- Automatically migrate projects from the legacy `.claude-auto` directory to `.ketchup` on first plugin load, so existing setups upgrade seamlessly without manual intervention
