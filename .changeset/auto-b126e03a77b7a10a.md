---
"ketchup": minor
---

- Rebranded the project from Claude Auto to Ketchup, including the npm package, plugin manifest, slash commands, data directory, and documentation site
- Added automatic migration so existing projects upgrade seamlessly from .claude-auto to .ketchup, including legacy state files and deny-lists
- Consolidated configuration into a single .ketchup/ directory with a brand-aligned state.json filename
- Switched to plugin-only installation mode, removing the legacy npx CLI and adding marketplace support for easier setup
- Rewrote homepage, README, and documentation around the guardrail engineering thesis with new visuals, operational concerns disclosure, and a working validator demo
