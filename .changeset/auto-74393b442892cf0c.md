---
"ketchup": patch
---

- Removed the redundant Claude Auto Core Reminder, since validators already enforce the same rules at commit time and the per-prompt reminder continues to surface the workflow
