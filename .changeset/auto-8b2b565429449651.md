---
"ketchup": minor
---

- Added support for embedding the viewer in external hosts via a configurable API base URL
- Reverted in-progress VS Code extension scaffolding, with that work continuing on a separate branch
